pipeline {
    agent {
      kubernetes {
            label 'jenkins-jenkins-agent'
            defaultContainer 'dind'
        }
    }

    stages {
        stage('ls') {
            steps {
                script {
                    dir('repo') {
                        sh '''ls'''
                    }
                }
            }
        }
    }
}